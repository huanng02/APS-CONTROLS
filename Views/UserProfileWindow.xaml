<Window x:Class="QuanLyGiuXe.Views.UserProfileWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="CÀI ĐẶT TÀI KHOẢN"
        SizeToContent="Height"
        MinHeight="550"
        Width="480"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        Background="{StaticResource BgLightBrush}">

    <Grid Margin="30">
        <Border Style="{StaticResource EnterpriseCardBorder}" Padding="35">
            <StackPanel>
                <!-- Header -->
                <TextBlock Text="THÔNG TIN CÁ NHÂN" Style="{StaticResource Heading1Style}" Foreground="{StaticResource APSBlueBrush}" Margin="0,0,0,30" HorizontalAlignment="Center"/>

                <!-- Avatar -->
                <Border Width="100" Height="100" CornerRadius="50" Background="{StaticResource APSBlueBrush}" HorizontalAlignment="Center" Margin="0,0,0,30">
                    <TextBlock Text="👤" FontSize="45" Foreground="White" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                </Border>

                <!-- Tên -->
                <TextBlock Text="HỌ VÀ TÊN" Style="{StaticResource BaseTextBlockStyle}" FontWeight="Bold" Margin="0,0,0,8"/>
                <TextBox Style="{StaticResource ModernTextBox}" Margin="0,0,0,20" Text="{Binding Ten}"/>

                <!-- Username -->
                <TextBlock Text="TÊN TÀI KHOẢN" Style="{StaticResource BaseTextBlockStyle}" FontWeight="Bold" Margin="0,0,0,8"/>
                <TextBox Style="{StaticResource ModernTextBox}" Margin="0,0,0,30" Text="{Binding Username}" IsReadOnly="True" Opacity="0.7"/>

                <!-- Buttons -->
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="2*"/>
                        <ColumnDefinition Width="15"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <Button Grid.Column="0" Content="LƯU THAY ĐỔI" Style="{StaticResource ModernButton}" Background="{StaticResource SuccessBrush}" Height="45" Command="{Binding SaveCommand}"/>
                    <Button Grid.Column="2" Content="ĐÓNG" Style="{StaticResource ModernOutlineButton}" Height="45" Click="Close_Click"/>
                </Grid>
            </StackPanel>
        </Border>
    </Grid>
</Window>
