<Window x:Class="QuanLyGiuXe.Views.SQLQueryToolWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:QuanLyGiuXe.Views"
        mc:Ignorable="d"
        Title="SQL Query Tool - Quản trị hệ thống" Height="750" Width="1100"
        WindowStartupLocation="CenterScreen" Background="#F1F5F9">
    
    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="200"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="SQL QUERY TOOL" FontSize="20" FontWeight="Bold" Foreground="#1E293B" Margin="0,0,0,15"/>

        <!-- Editor Section -->
        <Border Grid.Row="1" Background="White" CornerRadius="8" BorderBrush="#CBD5E1" BorderThickness="1">
            <TextBox Text="{Binding SqlText, UpdateSourceTrigger=PropertyChanged}" 
                     AcceptsReturn="True" AcceptsTab="True"
                     TextWrapping="Wrap" FontFamily="Consolas" FontSize="14"
                     Padding="12" VerticalScrollBarVisibility="Auto" 
                     BorderThickness="0" Background="Transparent">
                <TextBox.InputBindings>
                    <KeyBinding Key="F5" Command="{Binding ExecuteCommand}"/>
                </TextBox.InputBindings>
            </TextBox>
        </Border>

        <!-- Action Bar -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,15">
            <Button Content="Thực thi (F5)" Command="{Binding ExecuteCommand}" Width="140" Height="40" 
                    Background="#2563EB" Foreground="White" FontWeight="Bold" Cursor="Hand">
                <Button.Resources>
                    <Style TargetType="Border">
                        <Setter Property="CornerRadius" Value="6"/>
                    </Style>
                </Button.Resources>
            </Button>
            
            <Button Content="Làm sạch" Command="{Binding ClearCommand}" Width="100" Height="40" 
                    Background="#94A3B8" Foreground="White" FontWeight="SemiBold" Margin="10,0,0,0" Cursor="Hand">
                <Button.Resources>
                    <Style TargetType="Border">
                        <Setter Property="CornerRadius" Value="6"/>
                    </Style>
                </Button.Resources>
            </Button>
        </StackPanel>

        <!-- Error Panel -->
        <Border Grid.Row="3" Background="#FEF2F2" BorderBrush="#FEE2E2" BorderThickness="1" 
                Padding="12" CornerRadius="6" Margin="0,0,0,15"
                Visibility="{Binding HasError, Converter={StaticResource BoolToVis}}">
            <TextBlock Text="{Binding ErrorMessage}" Foreground="#B91C1C" FontWeight="SemiBold" 
                       FontSize="13" TextWrapping="Wrap"/>
        </Border>

        <!-- Result Grid -->
        <Border Grid.Row="4" Background="White" CornerRadius="8" BorderBrush="#E2E8F0" BorderThickness="1" Padding="1">
            <DataGrid ItemsSource="{Binding QueryResult}" AutoGenerateColumns="True" IsReadOnly="True"
                      Background="Transparent" BorderThickness="0" GridLinesVisibility="Horizontal"
                      HorizontalGridLinesBrush="#F1F5F9" VerticalGridLinesBrush="#F1F5F9"
                      ColumnHeaderHeight="40" RowHeight="35" SelectionUnit="FullRow">
                <DataGrid.Resources>
                    <Style TargetType="DataGridColumnHeader">
                        <Setter Property="Background" Value="#F8FAFC"/>
                        <Setter Property="Foreground" Value="#475569"/>
                        <Setter Property="FontWeight" Value="Bold"/>
                        <Setter Property="Padding" Value="12,0"/>
                        <Setter Property="BorderThickness" Value="0,0,1,1"/>
                        <Setter Property="BorderBrush" Value="#E2E8F0"/>
                        <Setter Property="HorizontalContentAlignment" Value="Left"/>
                    </Style>
                    <Style TargetType="DataGridCell">
                        <Setter Property="Padding" Value="12,0"/>
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="DataGridCell">
                                    <Border Padding="{TemplateBinding Padding}" Background="{TemplateBinding Background}">
                                        <ContentPresenter VerticalAlignment="Center" />
                                    </Border>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                        <Style.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter Property="Background" Value="#E0F2FE"/>
                                <Setter Property="Foreground" Value="#0369A1"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </DataGrid.Resources>
            </DataGrid>
        </Border>

        <!-- Status Bar -->
        <Border Grid.Row="5" Background="#F8FAFC" BorderBrush="#E2E8F0" BorderThickness="0,1,0,0" Padding="10,5" Margin="-20,5,-20,-20">
            <TextBlock Text="{Binding StatusMessage}" Foreground="#64748B" FontSize="12" FontWeight="Medium"/>
        </Border>
    </Grid>
</Window>
