<UserControl x:Class="QuanLyGiuXe.Views.Components.ConnectivityStatusChip"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:QuanLyGiuXe.Views.Components"
             mc:Ignorable="d" 
             d:DesignHeight="40" d:DesignWidth="150">
    
    <UserControl.Resources>
        <!-- Pulse Animation for Reconnecting state -->
        <Storyboard x:Key="PulseAnimation" RepeatBehavior="Forever">
            <DoubleAnimation Storyboard.TargetName="GlowCircle"
                             Storyboard.TargetProperty="Opacity"
                             From="0.8" To="0.2" Duration="0:0:1" AutoReverse="True"/>
            <DoubleAnimation Storyboard.TargetName="GlowCircle"
                             Storyboard.TargetProperty="ScaleX"
                             From="1" To="1.5" Duration="0:0:1" AutoReverse="True"/>
            <DoubleAnimation Storyboard.TargetName="GlowCircle"
                             Storyboard.TargetProperty="ScaleY"
                             From="1" To="1.5" Duration="0:0:1" AutoReverse="True"/>
        </Storyboard>
    </UserControl.Resources>

    <Border Background="#26FFFFFF" CornerRadius="18" Padding="12,6" HorizontalAlignment="Left" VerticalAlignment="Center">
        <Border.Effect>
            <DropShadowEffect BlurRadius="10" Opacity="0.1" ShadowDepth="1"/>
        </Border.Effect>
        <StackPanel Orientation="Horizontal">
            <Grid VerticalAlignment="Center" Margin="0,0,10,0">
                <!-- Outer Glow -->
                <Ellipse x:Name="GlowCircle" Width="10" Height="10" Fill="{Binding StatusColor}">
                    <Ellipse.RenderTransform>
                        <ScaleTransform x:Name="GlowScale" CenterX="5" CenterY="5"/>
                    </Ellipse.RenderTransform>
                </Ellipse>
                
                <!-- Main Indicator -->
                <Ellipse Width="10" Height="10" Fill="{Binding StatusColor}" Stroke="White" StrokeThickness="1.5"/>
            </Grid>

            <TextBlock Text="{Binding StatusText}" 
                       Foreground="White" 
                       FontSize="11" 
                       FontWeight="Bold" 
                       VerticalAlignment="Center"/>
        </StackPanel>

        <Border.Style>
            <Style TargetType="Border">
                <Style.Triggers>
                    <DataTrigger Binding="{Binding IsReconnecting}" Value="True">
                        <DataTrigger.EnterActions>
                            <BeginStoryboard Storyboard="{StaticResource PulseAnimation}" Name="StartPulse"/>
                        </DataTrigger.EnterActions>
                        <DataTrigger.ExitActions>
                            <StopStoryboard BeginStoryboardName="StartPulse"/>
                        </DataTrigger.ExitActions>
                    </DataTrigger>
                </Style.Triggers>
            </Style>
        </Border.Style>
    </Border>
</UserControl>
