<UserControl x:Class="QuanLyGiuXe.Views.DatabaseExplorerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="700" d:DesignWidth="1100" Background="#F4F7F6" FontFamily="Segoe UI">
    
    <UserControl.Resources>
        <!-- Color Palette -->
        <SolidColorBrush x:Key="PrimaryColor" Color="#2B3A4A"/>
        <SolidColorBrush x:Key="SecondaryColor" Color="#4A90E2"/>
        <SolidColorBrush x:Key="AccentColor" Color="#27AE60"/>
        <SolidColorBrush x:Key="DangerColor" Color="#E74C3C"/>
        <SolidColorBrush x:Key="WarningColor" Color="#F39C12"/>
        <SolidColorBrush x:Key="BgColor" Color="#F4F7F6"/>
        <SolidColorBrush x:Key="CardBgColor" Color="#FFFFFF"/>
        <SolidColorBrush x:Key="BorderColor" Color="#DDE3E8"/>
        <SolidColorBrush x:Key="TextColor" Color="#333333"/>
        <SolidColorBrush x:Key="MutedTextColor" Color="#888888"/>

        <!-- Button Styles -->
        <Style x:Key="FlatButton" TargetType="Button">
            <Setter Property="Background" Value="{StaticResource PrimaryColor}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="15,8"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="4">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Opacity" Value="0.9"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Opacity" Value="0.8"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ActionButton" BasedOn="{StaticResource FlatButton}" TargetType="Button">
            <Setter Property="Background" Value="{StaticResource SecondaryColor}"/>
        </Style>

        <Style x:Key="DangerButton" BasedOn="{StaticResource FlatButton}" TargetType="Button">
            <Setter Property="Background" Value="{StaticResource DangerColor}"/>
        </Style>
        
        <Style x:Key="SuccessButton" BasedOn="{StaticResource FlatButton}" TargetType="Button">
            <Setter Property="Background" Value="{StaticResource AccentColor}"/>
        </Style>

        <!-- ListBox Style for Sidebar -->
        <Style TargetType="ListBox">
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Disabled"/>
            <Setter Property="ItemContainerStyle">
                <Setter.Value>
                    <Style TargetType="ListBoxItem">
                        <Setter Property="Padding" Value="15,10"/>
                        <Setter Property="Margin" Value="0,2"/>
                        <Setter Property="Cursor" Value="Hand"/>
                        <Setter Property="Foreground" Value="{StaticResource TextColor}"/>
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="ListBoxItem">
                                    <Border x:Name="Border" Background="Transparent" CornerRadius="4">
                                        <ContentPresenter Margin="{TemplateBinding Padding}"/>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter TargetName="Border" Property="Background" Value="#E6EDF2"/>
                                        </Trigger>
                                        <Trigger Property="IsSelected" Value="True">
                                            <Setter TargetName="Border" Property="Background" Value="{StaticResource SecondaryColor}"/>
                                            <Setter Property="Foreground" Value="White"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- DataGrid Style -->
        <Style TargetType="DataGrid">
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderColor}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="RowBackground" Value="White"/>
            <Setter Property="AlternatingRowBackground" Value="#FBFBFC"/>
            <Setter Property="HorizontalGridLinesBrush" Value="{StaticResource BorderColor}"/>
            <Setter Property="VerticalGridLinesBrush" Value="Transparent"/>
            <Setter Property="RowHeaderWidth" Value="0"/>
            <Setter Property="HeadersVisibility" Value="Column"/>
            <Setter Property="GridLinesVisibility" Value="Horizontal"/>
            <Setter Property="CanUserAddRows" Value="False"/>
            <Setter Property="ColumnHeaderStyle">
                <Setter.Value>
                    <Style TargetType="DataGridColumnHeader">
                        <Setter Property="Background" Value="{StaticResource PrimaryColor}"/>
                        <Setter Property="Foreground" Value="White"/>
                        <Setter Property="Padding" Value="10,8"/>
                        <Setter Property="FontWeight" Value="SemiBold"/>
                        <Setter Property="BorderThickness" Value="0,0,1,0"/>
                        <Setter Property="BorderBrush" Value="#3D4F61"/>
                    </Style>
                </Setter.Value>
            </Setter>
            <Setter Property="CellStyle">
                <Setter.Value>
                    <Style TargetType="DataGridCell">
                        <Setter Property="Padding" Value="10,6"/>
                        <Setter Property="BorderThickness" Value="0"/>
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="DataGridCell">
                                    <Border Background="{TemplateBinding Background}" Padding="{TemplateBinding Padding}">
                                        <ContentPresenter VerticalAlignment="Center"/>
                                    </Border>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                        <Style.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter Property="Background" Value="#EBF3FA"/>
                                <Setter Property="Foreground" Value="{StaticResource TextColor}"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- TabControl Style -->
        <Style TargetType="TabControl">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
        </Style>
        <Style TargetType="TabItem">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{StaticResource MutedTextColor}"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Padding" Value="20,10"/>
            <Setter Property="Margin" Value="0,0,5,0"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TabItem">
                        <Border x:Name="Border" Background="{TemplateBinding Background}" BorderBrush="{StaticResource SecondaryColor}" BorderThickness="0,0,0,3" CornerRadius="4,4,0,0">
                            <ContentPresenter x:Name="ContentSite" VerticalAlignment="Center" HorizontalAlignment="Center" ContentSource="Header" Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="{StaticResource CardBgColor}"/>
                                <Setter Property="Foreground" Value="{StaticResource SecondaryColor}"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="False">
                                <Setter TargetName="Border" Property="BorderThickness" Value="0"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Foreground" Value="{StaticResource SecondaryColor}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid Margin="15">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="280" />
            <ColumnDefinition Width="15" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <!-- Sidebar: Danh sách bảng -->
        <Border Grid.Column="0" Background="{StaticResource CardBgColor}" CornerRadius="8" Padding="15">
            <Border.Effect>
                <DropShadowEffect Color="#000000" Opacity="0.05" BlurRadius="10" ShadowDepth="2"/>
            </Border.Effect>
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="15" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>
                
                <Grid Grid.Row="0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                        <TextBlock Text="🗄️" FontSize="18" Margin="0,0,8,0" VerticalAlignment="Center"/>
                        <TextBlock Text="Database Tables" FontWeight="Bold" FontSize="16" Foreground="{StaticResource PrimaryColor}" VerticalAlignment="Center" />
                    </StackPanel>
                    <Button Grid.Column="1" Command="{Binding RefreshCommand}" Style="{StaticResource FlatButton}" Background="#E6EDF2" Foreground="{StaticResource PrimaryColor}" Padding="8,4" ToolTip="Làm mới danh sách">
                        <TextBlock Text="↻ Refresh" FontSize="12" FontWeight="SemiBold"/>
                    </Button>
                </Grid>

                <TextBlock Grid.Row="1" Text="{Binding ConnectedServerInfo}" Foreground="{StaticResource AccentColor}" FontSize="12" FontWeight="SemiBold" Margin="0,5,0,0" />
                
                <Border Grid.Row="2" BorderBrush="{StaticResource BorderColor}" BorderThickness="0,0,0,1" Margin="0,0,0,5"/>
                
                <ListBox Grid.Row="3" ItemsSource="{Binding Tables}" SelectedItem="{Binding SelectedTable}">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="📋" FontSize="14" Margin="0,0,8,0" VerticalAlignment="Center"/>
                                <TextBlock Text="{Binding}" VerticalAlignment="Center" FontWeight="Medium"/>
                            </StackPanel>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </Grid>
        </Border>

        <!-- Splitter -->
        <GridSplitter Grid.Column="1" Width="5" HorizontalAlignment="Center" Background="Transparent" />

        <!-- Main Content: Tabs -->
        <Border Grid.Column="2" Background="{StaticResource CardBgColor}" CornerRadius="8" Padding="10">
            <Border.Effect>
                <DropShadowEffect Color="#000000" Opacity="0.05" BlurRadius="10" ShadowDepth="2"/>
            </Border.Effect>
            <TabControl>
                <!-- DATA TAB -->
                <TabItem Header="Dữ Liệu">
                    <Grid Margin="0,15,0,0">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="*" />
                        </Grid.RowDefinitions>
                        
                        <Border Grid.Row="0" Background="#F8FAFC" BorderBrush="{StaticResource BorderColor}" BorderThickness="1" CornerRadius="4" Padding="12,10" Margin="0,0,0,10">
                            <TextBlock Text="{Binding SelectedTable, StringFormat='Hiển thị dữ liệu của bảng: {0}'}" FontSize="14" Foreground="{StaticResource PrimaryColor}" FontWeight="SemiBold" />
                        </Border>
                        
                        <DataGrid Grid.Row="1" ItemsSource="{Binding CurrentData.DefaultView}" IsReadOnly="True" />
                    </Grid>
                </TabItem>

                <!-- SCHEMA TAB -->
                <TabItem Header="Cấu Trúc Cột">
                    <Grid Margin="0,15,0,0">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="*" />
                        </Grid.RowDefinitions>
                        
                        <Border Grid.Row="0" Background="#F8FAFC" BorderBrush="{StaticResource BorderColor}" BorderThickness="1" CornerRadius="4" Padding="12,10" Margin="0,0,0,10">
                            <TextBlock Text="{Binding SelectedTable, StringFormat='Cấu trúc Schema của bảng: {0}'}" FontSize="14" Foreground="{StaticResource PrimaryColor}" FontWeight="SemiBold" />
                        </Border>

                        <DataGrid Grid.Row="1" ItemsSource="{Binding CurrentSchema.DefaultView}" IsReadOnly="True" />
                    </Grid>
                </TabItem>

                <!-- QUERY EDITOR TAB -->
                <TabItem Header="Tạo Query &amp; Chạy">
                    <Grid Margin="0,15,0,0">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="*" />
                            <RowDefinition Height="15" />
                            <RowDefinition Height="*" />
                        </Grid.RowDefinitions>

                        <!-- Action Bar -->
                        <Border Grid.Row="0" Background="#F8FAFC" BorderBrush="{StaticResource BorderColor}" BorderThickness="1" CornerRadius="4" Padding="10" Margin="0,0,0,10">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>
                                
                                <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                                    <TextBlock Text="Sinh tự động:" VerticalAlignment="Center" Margin="0,0,10,0" Foreground="{StaticResource MutedTextColor}" FontWeight="SemiBold" />
                                    <Button Content="SELECT" Command="{Binding GenerateSelectCommand}" Style="{StaticResource ActionButton}" Margin="0,0,5,0" />
                                    <Button Content="INSERT" Command="{Binding GenerateInsertCommand}" Style="{StaticResource ActionButton}" Margin="0,0,5,0" />
                                    <Button Content="UPDATE" Command="{Binding GenerateUpdateCommand}" Style="{StaticResource ActionButton}" Margin="0,0,5,0" />
                                    <Button Content="DELETE" Command="{Binding GenerateDeleteCommand}" Style="{StaticResource DangerButton}" />
                                </StackPanel>
                                
                                <Button Grid.Column="2" Command="{Binding ExecuteQueryCommand}" Style="{StaticResource SuccessButton}">
                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock Text="▶" Margin="0,0,5,0"/>
                                        <TextBlock Text="Thực thi Query (F5)"/>
                                    </StackPanel>
                                </Button>
                            </Grid>
                        </Border>

                        <!-- Editor -->
                        <Border Grid.Row="1" BorderBrush="{StaticResource BorderColor}" BorderThickness="1" CornerRadius="4" Background="#1E1E1E">
                            <TextBox Text="{Binding QueryText, UpdateSourceTrigger=PropertyChanged}" 
                                     AcceptsReturn="True" AcceptsTab="True" 
                                     FontFamily="Consolas" FontSize="15" Foreground="#D4D4D4"
                                     Background="Transparent" BorderThickness="0"
                                     VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto" 
                                     Padding="15" CaretBrush="White" />
                        </Border>

                        <!-- Splitter -->
                        <GridSplitter Grid.Row="2" Height="5" HorizontalAlignment="Stretch" VerticalAlignment="Center" Background="Transparent" />

                        <!-- Result Grid -->
                        <Border Grid.Row="3" BorderBrush="{StaticResource BorderColor}" BorderThickness="1" CornerRadius="4">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="*"/>
                                </Grid.RowDefinitions>
                                <Border Background="{StaticResource PrimaryColor}" Padding="10,6" CornerRadius="3,3,0,0">
                                    <TextBlock Text="Kết quả truy vấn" Foreground="White" FontWeight="SemiBold" FontSize="12"/>
                                </Border>
                                <DataGrid Grid.Row="1" ItemsSource="{Binding QueryResult.DefaultView}" IsReadOnly="True" BorderThickness="0" />
                            </Grid>
                        </Border>
                    </Grid>
                </TabItem>
            </TabControl>
        </Border>
    </Grid>
</UserControl>
