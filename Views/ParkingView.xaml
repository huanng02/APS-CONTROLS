<UserControl x:Class="QuanLyGiuXe.Views.ParkingView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:QuanLyGiuXe.Views"
             mc:Ignorable="d" 
             d:DesignHeight="800" d:DesignWidth="1000"
             Background="Transparent">
    
    <Grid Margin="0">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="8"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- ENTRANCE LANE (VÀO) -->
        <Border Grid.Column="0" Style="{StaticResource EnterpriseCardBorder}" Padding="0">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <Border Background="{StaticResource BgLightBrush}" Padding="15,10" CornerRadius="4,4,0,0" BorderBrush="{StaticResource BorderLightBrush}" BorderThickness="0,0,0,1">
                    <TextBlock Text="LÀN VÀO (INBOUND)" FontWeight="Bold" Foreground="{StaticResource APSBlueBrush}"/>
                </Border>

                <Grid Grid.Row="1" Margin="15">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <!-- Camera 1 & Snap 1 -->
                    <Grid Grid.Row="0" Margin="0,0,0,8">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Border Background="Black" CornerRadius="4" Margin="0,0,4,0">
                            <Grid>
                                <Image x:Name="CameraVao1" Stretch="UniformToFill"/>
                                <Border Background="#80000000" VerticalAlignment="Top" HorizontalAlignment="Left" Padding="5,2" Margin="5">
                                    <TextBlock Text="CAM 1 - LIVE" Foreground="White" FontSize="10" FontWeight="Bold"/>
                                </Border>
                            </Grid>
                        </Border>
                        <Border Grid.Column="1" Background="Black" CornerRadius="4" Margin="4,0,0,0">
                            <Grid>
                                <Image Source="{Binding AnhChupVao1}" Stretch="UniformToFill"/>
                                <Border Background="#8027AE60" VerticalAlignment="Top" HorizontalAlignment="Left" Padding="5,2" Margin="5">
                                    <TextBlock Text="SNAP 1" Foreground="White" FontSize="10" FontWeight="Bold"/>
                                </Border>
                            </Grid>
                        </Border>
                    </Grid>

                    <!-- Camera 2 & Snap 2 -->
                    <Grid Grid.Row="1" Margin="0,0,0,8">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Border Background="Black" CornerRadius="4" Margin="0,0,4,0">
                            <Grid>
                                <Image x:Name="CameraVao2" Stretch="UniformToFill"/>
                                <Border Background="#80000000" VerticalAlignment="Top" HorizontalAlignment="Left" Padding="5,2" Margin="5">
                                    <TextBlock Text="CAM 2 - LIVE" Foreground="White" FontSize="10" FontWeight="Bold"/>
                                </Border>
                            </Grid>
                        </Border>
                        <Border Grid.Column="1" Background="Black" CornerRadius="4" Margin="4,0,0,0">
                            <Grid>
                                <Image Source="{Binding AnhChupVao2}" Stretch="UniformToFill"/>
                                <Border Background="#8027AE60" VerticalAlignment="Top" HorizontalAlignment="Left" Padding="5,2" Margin="5">
                                    <TextBlock Text="SNAP 2" Foreground="White" FontSize="10" FontWeight="Bold"/>
                                </Border>
                            </Grid>
                        </Border>
                    </Grid>

                    <StackPanel Grid.Row="2">
                        <Border Background="{StaticResource BgLightBrush}" Padding="15" CornerRadius="4">
                            <StackPanel>
                                <TextBlock Text="THÔNG TIN XE VÀO" Style="{StaticResource SubTitleStyle}" Margin="0,0,0,10"/>
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <StackPanel>
                                        <TextBlock Text="Biển số:" FontSize="11" Foreground="{StaticResource TextSecondaryBrush}"/>
                                        <TextBlock Text="{Binding LanVaoBienSo}" FontSize="24" FontWeight="Bold" Foreground="{StaticResource APSBlueBrush}"/>
                                    </StackPanel>
                                    <Button Grid.Column="1" Content="MỞ CỔNG" Style="{StaticResource ModernButton}" Click="OpenGateIn_Click" Width="100"/>
                                </Grid>
                                <TextBlock Text="{Binding LanVaoTrangThai}" Margin="0,10,0,0" Foreground="{StaticResource SuccessBrush}" FontWeight="SemiBold"/>
                            </StackPanel>
                        </Border>
                    </StackPanel>
                </Grid>
            </Grid>
        </Border>

        <!-- SPLITTER -->
        <GridSplitter Grid.Column="1" Width="8" Background="Transparent" HorizontalAlignment="Stretch" />

        <!-- EXIT LANE (RA) -->
        <Border Grid.Column="2" Style="{StaticResource EnterpriseCardBorder}" Padding="0">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <Border Background="{StaticResource BgLightBrush}" Padding="15,10" CornerRadius="4,4,0,0" BorderBrush="{StaticResource BorderLightBrush}" BorderThickness="0,0,0,1">
                    <TextBlock Text="LÀN RA (OUTBOUND)" FontWeight="Bold" Foreground="{StaticResource APSRedBrush}"/>
                </Border>

                <Grid Grid.Row="1" Margin="15">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <!-- Camera 1 & Snap 1 -->
                    <Grid Grid.Row="0" Margin="0,0,0,8">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Border Background="Black" CornerRadius="4" Margin="0,0,4,0">
                            <Grid>
                                <Image x:Name="CameraRa1" Stretch="UniformToFill"/>
                                <Border Background="#80000000" VerticalAlignment="Top" HorizontalAlignment="Left" Padding="5,2" Margin="5">
                                    <TextBlock Text="CAM 1 - LIVE" Foreground="White" FontSize="10" FontWeight="Bold"/>
                                </Border>
                            </Grid>
                        </Border>
                        <Border Grid.Column="1" Background="Black" CornerRadius="4" Margin="4,0,0,0">
                            <Grid>
                                <Image Source="{Binding AnhChupRa1}" Stretch="UniformToFill"/>
                                <Border Background="#80E74C3C" VerticalAlignment="Top" HorizontalAlignment="Left" Padding="5,2" Margin="5">
                                    <TextBlock Text="SNAP 1" Foreground="White" FontSize="10" FontWeight="Bold"/>
                                </Border>
                            </Grid>
                        </Border>
                    </Grid>

                    <!-- Camera 2 & Snap 2 -->
                    <Grid Grid.Row="1" Margin="0,0,0,8">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Border Background="Black" CornerRadius="4" Margin="0,0,4,0">
                            <Grid>
                                <Image x:Name="CameraRa2" Stretch="UniformToFill"/>
                                <Border Background="#80000000" VerticalAlignment="Top" HorizontalAlignment="Left" Padding="5,2" Margin="5">
                                    <TextBlock Text="CAM 2 - LIVE" Foreground="White" FontSize="10" FontWeight="Bold"/>
                                </Border>
                            </Grid>
                        </Border>
                        <Border Grid.Column="1" Background="Black" CornerRadius="4" Margin="4,0,0,0">
                            <Grid>
                                <Image Source="{Binding AnhChupRa2}" Stretch="UniformToFill"/>
                                <Border Background="#80E74C3C" VerticalAlignment="Top" HorizontalAlignment="Left" Padding="5,2" Margin="5">
                                    <TextBlock Text="SNAP 2" Foreground="White" FontSize="10" FontWeight="Bold"/>
                                </Border>
                            </Grid>
                        </Border>
                    </Grid>

                    <StackPanel Grid.Row="2">
                        <Border Background="{StaticResource BgLightBrush}" Padding="15" CornerRadius="4">
                            <StackPanel>
                                <TextBlock Text="THÔNG TIN XE RA" Style="{StaticResource SubTitleStyle}" Margin="0,0,0,10"/>
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <StackPanel>
                                        <TextBlock Text="Biển số:" FontSize="11" Foreground="{StaticResource TextSecondaryBrush}"/>
                                        <TextBlock Text="{Binding LanRaBienSo}" FontSize="24" FontWeight="Bold" Foreground="{StaticResource APSRedBrush}"/>
                                    </StackPanel>
                                    <Button Grid.Column="1" Content="MỞ CỔNG" Style="{StaticResource DangerButton}" Click="OpenGateOut_Click" Width="100"/>
                                </Grid>

                                <Grid Margin="0,10,0,0">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Text="{Binding LanRaTrangThai}" Foreground="{StaticResource WarningBrush}" FontWeight="SemiBold" VerticalAlignment="Center"/>
                                    <TextBlock Grid.Column="1" Text="{Binding LanRaTien, StringFormat='{}PHÍ: {0}đ'}" FontSize="18" FontWeight="Bold" Foreground="{StaticResource SuccessBrush}"/>
                                </Grid>
                            </StackPanel>
                        </Border>
                    </StackPanel>
                </Grid>
            </Grid>
        </Border>
    </Grid>
</UserControl>

