<UserControl x:Class="QuanLyGiuXe.Views.ExcelImportExportView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="clr-namespace:QuanLyGiuXe.ViewModels"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="1000">

    <!-- DataContext is set in code-behind to allow safe construction with try/catch -->

    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- TOOLBAR -->
        <Border Grid.Row="0" Style="{StaticResource EnterpriseCardBorder}" Padding="15" Margin="0,0,0,15">
            <StackPanel Orientation="Horizontal">
                <Button Content="📂 IMPORT EXCEL" Command="{Binding ImportExcelCommand}" Style="{StaticResource ModernButton}" Background="{StaticResource APSBlueBrush}" Width="160" Margin="0,0,10,0" />
                <Button Content="📥 EXPORT EXCEL" Command="{Binding ExportExcelCommand}" Style="{StaticResource ModernOutlineButton}" Width="160" />
            </StackPanel>
        </Border>

        <!-- PROGRESS -->
        <Border Grid.Row="1" Margin="0,0,0,15" Visibility="{Binding Progress, Converter={StaticResource BoolToVis}}">
             <ProgressBar Height="4" Minimum="0" Maximum="100" Value="{Binding Progress}" Background="{StaticResource BgLightBrush}" Foreground="{StaticResource APSBlueBrush}"/>
        </Border>

        <!-- PREVIEW DATA -->
        <Border Grid.Row="2" Style="{StaticResource EnterpriseCardBorder}" Padding="0">
            <DataGrid ItemsSource="{Binding PreviewRows}" AutoGenerateColumns="False" IsReadOnly="True" Style="{StaticResource ModernDataGrid}">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="CARD UID" Binding="{Binding CardUID}" Width="*"/>
                    <DataGridTextColumn Header="PLATE NUMBER" Binding="{Binding BienSo}" Width="150" />
                    <DataGridTextColumn Header="VEHICLE ID" Binding="{Binding LoaiXeId}" Width="120" />
                    <DataGridTextColumn Header="TICKET ID" Binding="{Binding LoaiVeId}" Width="120" />
                    <DataGridTextColumn Header="ISSUE DATE" Binding="{Binding NgayDangKy, StringFormat=dd/MM/yyyy}" Width="150" />
                    <DataGridTextColumn Header="EXPIRY DATE" Binding="{Binding NgayHetHan, StringFormat=dd/MM/yyyy}" Width="150" />
                    <DataGridTextColumn Header="STATUS" Binding="{Binding Status}" Width="150" />
                    <DataGridTextColumn Header="MESSAGE" Binding="{Binding StatusMessage}" Width="*" />
                </DataGrid.Columns>
            </DataGrid>
        </Border>

        <!-- ACTIONS -->
        <Border Grid.Row="3" Style="{StaticResource EnterpriseCardBorder}" Padding="15" Margin="0,15,0,0">
            <Grid>
                <TextBlock Text="{Binding StatusSummary}" Style="{StaticResource BaseTextBlockStyle}" FontWeight="Bold" VerticalAlignment="Center" HorizontalAlignment="Left" Foreground="{StaticResource APSBlueBrush}"/>
                <Button Content="✅ CONFIRM IMPORT" Command="{Binding ConfirmImportCommand}" Style="{StaticResource ModernButton}" Background="{StaticResource SuccessBrush}" Width="200" HorizontalAlignment="Right" />
            </Grid>
        </Border>
    </Grid>
</UserControl>

