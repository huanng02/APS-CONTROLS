<UserControl x:Class="QuanLyGiuXe.Views.ParkingTopologyView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:viewmodels="clr-namespace:QuanLyGiuXe.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="1000"
             Background="{StaticResource BgLightBrush}">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="CẤU HÌNH HARDWARE &amp; TOPOLOGY" Style="{StaticResource Heading1Style}" Margin="0,0,0,20" Foreground="{StaticResource APSBlueBrush}"/>

        <TabControl Grid.Row="1" Style="{StaticResource EnterpriseTabControl}">
            <!-- SITES TAB -->
            <TabItem Header="SITES (BÃI XE)">
                <Grid Margin="10">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,15">
                        <Button Content="+ Thêm Site" Command="{Binding AddSiteCommand}" Style="{StaticResource ModernButton}" Margin="0,0,10,0"/>
                        <Button Content="Sửa" Command="{Binding EditSiteCommand}" Style="{StaticResource ModernOutlineButton}" Margin="0,0,10,0"/>
                        <Button Content="Xóa" Command="{Binding DeleteSiteCommand}" Style="{StaticResource ModernOutlineButton}" Foreground="{StaticResource ErrorBrush}" BorderBrush="{StaticResource ErrorBrush}"/>
                    </StackPanel>

                    <DataGrid Grid.Row="1" ItemsSource="{Binding Sites}" SelectedItem="{Binding SelectedSite}" Style="{StaticResource ModernDataGrid}" AutoGenerateColumns="False">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="50" IsReadOnly="True"/>
                            <DataGridTextColumn Header="Mã Site" Binding="{Binding SiteCode}" Width="150" IsReadOnly="True"/>
                            <DataGridTextColumn Header="Tên Site" Binding="{Binding SiteName}" Width="250" IsReadOnly="True"/>
                            <DataGridTextColumn Header="Mô tả" Binding="{Binding Description}" Width="*" IsReadOnly="True"/>
                            <DataGridCheckBoxColumn Header="Trạng thái" Binding="{Binding IsActive}" Width="100" IsReadOnly="True"/>
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </TabItem>

            <!-- ZONES TAB -->
            <TabItem Header="ZONES (KHU VỰC)">
                <Grid Margin="10">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,15">
                        <Button Content="+ Thêm Zone" Command="{Binding AddZoneCommand}" Style="{StaticResource ModernButton}" Margin="0,0,10,0"/>
                        <Button Content="Sửa" Command="{Binding EditZoneCommand}" Style="{StaticResource ModernOutlineButton}" Margin="0,0,10,0"/>
                        <Button Content="Xóa" Command="{Binding DeleteZoneCommand}" Style="{StaticResource ModernOutlineButton}" Foreground="{StaticResource ErrorBrush}" BorderBrush="{StaticResource ErrorBrush}"/>
                    </StackPanel>

                    <DataGrid Grid.Row="1" ItemsSource="{Binding Zones}" SelectedItem="{Binding SelectedZone}" Style="{StaticResource ModernDataGrid}" AutoGenerateColumns="False">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="50" IsReadOnly="True"/>
                            <DataGridTextColumn Header="Site Name" Binding="{Binding SiteName}" Width="150" IsReadOnly="True"/>
                            <DataGridTextColumn Header="Mã Zone" Binding="{Binding ZoneCode}" Width="100" IsReadOnly="True"/>
                            <DataGridTextColumn Header="Tên Zone" Binding="{Binding ZoneName}" Width="200" IsReadOnly="True"/>
                            <DataGridTextColumn Header="Mô tả" Binding="{Binding Description}" Width="*" IsReadOnly="True"/>
                            <DataGridTextColumn Header="Sức chứa" Binding="{Binding MaxCapacity}" Width="100" IsReadOnly="True"/>
                            <DataGridCheckBoxColumn Header="Trạng thái" Binding="{Binding IsActive}" Width="100" IsReadOnly="True"/>
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </TabItem>

            <!-- LANES TAB -->
            <TabItem Header="LANES (LÀN XE)">
                <Grid Margin="10">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,15">
                        <Button Content="+ Thêm Làn" Command="{Binding AddLaneCommand}" Style="{StaticResource ModernButton}" Margin="0,0,10,0"/>
                        <Button Content="Sửa" Command="{Binding EditLaneCommand}" Style="{StaticResource ModernOutlineButton}" Margin="0,0,10,0"/>
                        <Button Content="Xóa" Command="{Binding DeleteLaneCommand}" Style="{StaticResource ModernOutlineButton}" Foreground="{StaticResource ErrorBrush}" BorderBrush="{StaticResource ErrorBrush}"/>
                    </StackPanel>

                    <DataGrid Grid.Row="1" ItemsSource="{Binding Lanes}" SelectedItem="{Binding SelectedLane}" Style="{StaticResource ModernDataGrid}" AutoGenerateColumns="False">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="50" IsReadOnly="True"/>
                            <DataGridTextColumn Header="Zone Name" Binding="{Binding ZoneName}" Width="150" IsReadOnly="True"/>
                            <DataGridTextColumn Header="Mã Làn" Binding="{Binding LaneCode}" Width="100" IsReadOnly="True"/>
                            <DataGridTextColumn Header="Tên Làn" Binding="{Binding LaneName}" Width="200" IsReadOnly="True"/>
                            <DataGridTextColumn Header="Chiều (IN/OUT)" Binding="{Binding Direction}" Width="120" IsReadOnly="True"/>
                            <DataGridCheckBoxColumn Header="Trạng thái" Binding="{Binding IsActive}" Width="100" IsReadOnly="True"/>
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </TabItem>

            <!-- CONTROLLERS TAB -->
            <TabItem Header="C3 CONTROLLERS">
                <Grid Margin="10">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,15">
                        <Button Content="+ Thêm Controller" Command="{Binding AddControllerCommand}" Style="{StaticResource ModernButton}" Margin="0,0,10,0"/>
                        <Button Content="Sửa" Command="{Binding EditControllerCommand}" Style="{StaticResource ModernOutlineButton}" Margin="0,0,10,0"/>
                        <Button Content="Xóa" Command="{Binding DeleteControllerCommand}" Style="{StaticResource ModernOutlineButton}" Foreground="{StaticResource ErrorBrush}" BorderBrush="{StaticResource ErrorBrush}"/>
                    </StackPanel>

                    <DataGrid Grid.Row="1" ItemsSource="{Binding Controllers}" SelectedItem="{Binding SelectedController}" Style="{StaticResource ModernDataGrid}" AutoGenerateColumns="False">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="50" IsReadOnly="True"/>
                            <DataGridTextColumn Header="Zone Name" Binding="{Binding ZoneName}" Width="150" IsReadOnly="True"/>
                            <DataGridTextColumn Header="Tên Controller" Binding="{Binding ControllerName}" Width="200" IsReadOnly="True"/>
                            <DataGridTextColumn Header="IP Address" Binding="{Binding IpAddress}" Width="150" IsReadOnly="True"/>
                            <DataGridCheckBoxColumn Header="Trạng thái" Binding="{Binding IsActive}" Width="100" IsReadOnly="True"/>
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </TabItem>
        </TabControl>
    </Grid>
</UserControl>
