<Window x:Class="QuanLyGiuXe.Views.RFIDGiaHanDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Gia hạn thẻ RFID" Height="280" Width="380"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize" Background="White">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="GIA HẠN THẺ" FontSize="18" FontWeight="Bold" Margin="0,0,0,15" Foreground="#1E293B"/>

        <!-- Thông tin thẻ -->
        <StackPanel Grid.Row="1" Margin="0,0,0,10">
            <TextBlock Text="{Binding CardInfo}" FontSize="13" Foreground="#64748B"/>
            <TextBlock Text="{Binding PlateInfo}" FontSize="13" Foreground="#64748B" Margin="0,2,0,0"/>
        </StackPanel>

        <!-- Input -->
        <TextBlock Grid.Row="2" Text="Số tháng gia hạn:" FontWeight="SemiBold" Margin="0,5,0,5"/>
        <TextBox x:Name="txtMonths" Grid.Row="3" Height="34" VerticalContentAlignment="Center" Padding="8,0" FontSize="14" FontWeight="Bold" Text="1"/>

        <!-- Error -->
        <TextBlock x:Name="txtError" Grid.Row="4" Foreground="#EF4444" FontSize="12" TextWrapping="Wrap" Margin="0,5,0,0"/>

        <!-- Buttons -->
        <StackPanel Grid.Row="5" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Xác nhận" Width="100" Height="36" Background="#10B981" Foreground="White" FontWeight="Bold" Click="BtnOk_Click" Margin="0,0,10,0">
                <Button.Resources>
                    <Style TargetType="Border">
                        <Setter Property="CornerRadius" Value="6"/>
                    </Style>
                </Button.Resources>
            </Button>
            <Button Content="Hủy" Width="80" Height="36" Background="#94A3B8" Foreground="White" FontWeight="Bold" Click="BtnCancel_Click">
                <Button.Resources>
                    <Style TargetType="Border">
                        <Setter Property="CornerRadius" Value="6"/>
                    </Style>
                </Button.Resources>
            </Button>
        </StackPanel>
    </Grid>
</Window>
