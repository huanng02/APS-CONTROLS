<Window x:Class="QuanLyGiuXe.Views.RFIDGiaHanHistoryWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:QuanLyGiuXe.Views"
        mc:Ignorable="d"
        Title="Lịch sử gia hạn thẻ" Height="600" Width="1000"
        WindowStartupLocation="CenterScreen" Background="#F8FAFC">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="LỊCH SỬ GIA HẠN THẺ RFID" FontSize="20" FontWeight="Bold" Margin="0,0,0,20" Foreground="#1E293B"/>

        <!-- Filter Bar -->
        <Border Grid.Row="1" Background="White" Padding="15" CornerRadius="8" Margin="0,0,0,20">
            <Border.Effect>
                <DropShadowEffect BlurRadius="10" ShadowDepth="1" Opacity="0.1"/>
            </Border.Effect>
            <StackPanel Orientation="Horizontal">
                <StackPanel Margin="0,0,15,0">
                    <TextBlock Text="Từ ngày" FontSize="12" Foreground="#64748B" Margin="0,0,0,5"/>
                    <DatePicker SelectedDate="{Binding FromDate}" Width="150" Height="32"/>
                </StackPanel>
                
                <StackPanel Margin="0,0,15,0">
                    <TextBlock Text="Đến ngày" FontSize="12" Foreground="#64748B" Margin="0,0,0,5"/>
                    <DatePicker SelectedDate="{Binding ToDate}" Width="150" Height="32"/>
                </StackPanel>

                <StackPanel Margin="0,0,15,0">
                    <TextBlock Text="Tìm kiếm (UID, Biển số...)" FontSize="12" Foreground="#64748B" Margin="0,0,0,5"/>
                    <TextBox Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" Width="250" Height="32" VerticalContentAlignment="Center" Padding="8,0">
                        <TextBox.InputBindings>
                            <KeyBinding Key="Enter" Command="{Binding SearchCommand}"/>
                        </TextBox.InputBindings>
                    </TextBox>
                </StackPanel>

                <Button Content="Tìm kiếm" Command="{Binding SearchCommand}" Width="100" Height="32" Margin="0,20,0,0" Background="#3B82F6" Foreground="White" FontWeight="Bold" Cursor="Hand">
                    <Button.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="6"/>
                        </Style>
                    </Button.Resources>
                </Button>

                <Button Content="Làm mới" Command="{Binding LoadCommand}" Width="100" Height="32" Margin="10,20,0,0" Background="#94A3B8" Foreground="White" FontWeight="Bold" Cursor="Hand">
                    <Button.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="6"/>
                        </Style>
                    </Button.Resources>
                </Button>
            </StackPanel>
        </Border>

        <!-- Data Grid -->
        <DataGrid Grid.Row="2" ItemsSource="{Binding History}" AutoGenerateColumns="False" IsReadOnly="True" 
                  Background="White" BorderThickness="1" BorderBrush="#E2E8F0" GridLinesVisibility="Horizontal" 
                  RowHeight="40" HorizontalGridLinesBrush="#F1F5F9" SelectionUnit="FullRow">
            <DataGrid.Resources>
                <Style TargetType="DataGridColumnHeader">
                    <Setter Property="Background" Value="#F1F5F9"/>
                    <Setter Property="Foreground" Value="#475569"/>
                    <Setter Property="FontWeight" Value="SemiBold"/>
                    <Setter Property="Padding" Value="12,8"/>
                    <Setter Property="Height" Value="45"/>
                    <Setter Property="BorderThickness" Value="0,0,0,1"/>
                    <Setter Property="BorderBrush" Value="#E2E8F0"/>
                </Style>
                <Style TargetType="DataGridCell">
                    <Setter Property="Padding" Value="12,0"/>
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="DataGridCell">
                                <Border Padding="{TemplateBinding Padding}" Background="{TemplateBinding Background}">
                                    <ContentPresenter VerticalAlignment="Center" />
                                </Border>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                    <Style.Triggers>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter Property="Background" Value="#E0F2FE"/>
                            <Setter Property="Foreground" Value="#0369A1"/>
                        </Trigger>
                    </Style.Triggers>
                </Style>
            </DataGrid.Resources>
            <DataGrid.Columns>
                <DataGridTextColumn Header="Card UID" Binding="{Binding CardUID}" Width="150"/>
                <DataGridTextColumn Header="Tên thẻ" Binding="{Binding CardName}" Width="*"/>
                <DataGridTextColumn Header="Biển số" Binding="{Binding BienSo}" Width="120"/>
                <DataGridTextColumn Header="Số tháng" Binding="{Binding SoThang}" Width="80">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="HorizontalAlignment" Value="Center"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>
                <DataGridTextColumn Header="Ngày gia hạn" Binding="{Binding NgayGiaHan, StringFormat='dd/MM/yyyy HH:mm'}" Width="160"/>
                <DataGridTextColumn Header="Hết hạn mới" Binding="{Binding NgayHetHanMoi, StringFormat='dd/MM/yyyy'}" Width="140">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="FontWeight" Value="SemiBold"/>
                            <Setter Property="Foreground" Value="#059669"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Window>
