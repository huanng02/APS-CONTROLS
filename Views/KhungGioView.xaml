<UserControl x:Class="QuanLyGiuXe.Views.KhungGioView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="{StaticResource BgLightBrush}">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Border Style="{StaticResource EnterpriseCardBorder}" Padding="20" Margin="0,0,0,15">
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                <Border Background="{StaticResource APSBlueBrush}" CornerRadius="4" Padding="8" Margin="0,0,15,0">
                    <TextBlock Text="⏰" Foreground="White" FontSize="16"/>
                </Border>
                <TextBlock Text="TIME SLOT CONFIGURATION" Style="{StaticResource Heading2Style}" Foreground="{StaticResource APSBlueBrush}" />
            </StackPanel>
        </Border>

        <!-- DataGrid -->
        <DataGrid Grid.Row="1" ItemsSource="{Binding Items}" SelectedItem="{Binding Selected}" 
                  Style="{StaticResource ModernDataGrid}"
                  AutoGenerateColumns="False" CanUserAddRows="False">

            <DataGrid.Columns>
                <DataGridTextColumn Header="SLOT NAME" Binding="{Binding TenKhungGio, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Width="*" />
                
                <DataGridTemplateColumn Header="START TIME" Width="150">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding GioBatDauText}" VerticalAlignment="Center" Margin="10,0" FontWeight="SemiBold" Foreground="{StaticResource APSBlueBrush}"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                    <DataGridTemplateColumn.CellEditingTemplate>
                        <DataTemplate>
                            <TextBox Text="{Binding GioBatDauText, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource ModernTextBox}" Height="30"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellEditingTemplate>
                </DataGridTemplateColumn>

                <DataGridTemplateColumn Header="END TIME" Width="150">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding GioKetThucText}" VerticalAlignment="Center" Margin="10,0" FontWeight="SemiBold" Foreground="{StaticResource APSBlueBrush}"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                    <DataGridTemplateColumn.CellEditingTemplate>
                        <DataTemplate>
                            <TextBox Text="{Binding GioKetThucText, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource ModernTextBox}" Height="30"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellEditingTemplate>
                </DataGridTemplateColumn>

                <DataGridCheckBoxColumn Header="OVERNIGHT" Binding="{Binding QuaDem, Mode=OneWay}" IsReadOnly="True" Width="120" />
                <DataGridCheckBoxColumn Header="ACTIVE" Binding="{Binding TrangThai, Mode=TwoWay}" Width="100" />
            </DataGrid.Columns>
        </DataGrid>

        <!-- Actions -->
        <Border Grid.Row="2" Style="{StaticResource EnterpriseCardBorder}" Padding="15" Margin="0,15,0,0">
            <Grid>
                <TextBlock Text="{Binding StatusMessage}" Foreground="{StaticResource SuccessBrush}" FontWeight="Bold" VerticalAlignment="Center" HorizontalAlignment="Left" />
                
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button Content="➕ ADD SLOT" Command="{Binding AddCommand}" Style="{StaticResource ModernButton}" Background="{StaticResource SuccessBrush}" Width="140" Height="36" Margin="0,0,10,0" />
                    <Button Content="💾 SAVE CHANGES" Command="{Binding UpdateCommand}" Style="{StaticResource ModernButton}" Background="{StaticResource APSBlueBrush}" Width="160" Height="36" Margin="0,0,10,0" />
                    <Button Content="🗑️ DELETE" Command="{Binding DeleteCommand}" Style="{StaticResource DangerButton}" Width="120" Height="36" />
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</UserControl>
