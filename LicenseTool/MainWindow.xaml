<Window x:Class="LicenseTool.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="C3 License Administrator" 
        Icon="/LicenseTool;component/Assets/app.ico"
        Height="680" Width="1450"
        WindowStartupLocation="CenterScreen"
        Background="#12131A"
        Foreground="#FFFFFF">

    <Window.Resources>
        <Style x:Key="ModernTextBox" TargetType="TextBox">
            <Setter Property="Background" Value="#1C1D26"/>
            <Setter Property="BorderBrush" Value="#2F313E"/>
            <Setter Property="Foreground" Value="#FFFFFF"/>
            <Setter Property="Padding" Value="8,6"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border x:Name="border" CornerRadius="4" BorderThickness="1" BorderBrush="{TemplateBinding BorderBrush}" Background="{TemplateBinding Background}">
                            <ScrollViewer x:Name="PART_ContentHost" Focusable="false" HorizontalScrollBarVisibility="Hidden" VerticalScrollBarVisibility="Hidden"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="true">
                                <Setter TargetName="border" Property="BorderBrush" Value="#00D2FF"/>
                            </Trigger>
                            <Trigger Property="IsFocused" Value="true">
                                <Setter TargetName="border" Property="BorderBrush" Value="#00D2FF"/>
                                <Setter TargetName="border" Property="Background" Value="#161720"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ModernButton" TargetType="Button">
            <Setter Property="Background" Value="#252736"/>
            <Setter Property="Foreground" Value="#FFFFFF"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="16,8"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" CornerRadius="4" Background="{TemplateBinding Background}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="true">
                                <Setter TargetName="border" Property="Opacity" Value="0.9"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="true">
                                <Setter TargetName="border" Property="Opacity" Value="0.75"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="false">
                                <Setter TargetName="border" Property="Background" Value="#1E202B"/>
                                <Setter Property="Foreground" Value="#4A4F63"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="AccentButton" TargetType="Button" BasedOn="{StaticResource ModernButton}">
            <Setter Property="Background">
                <Setter.Value>
                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                        <GradientStop Color="#00B4DB" Offset="0"/>
                        <GradientStop Color="#0083B0" Offset="1"/>
                    </LinearGradientBrush>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="DangerButton" TargetType="Button" BasedOn="{StaticResource ModernButton}">
            <Setter Property="Background" Value="#FF4D4D"/>
        </Style>
    </Window.Resources>

    <Grid Margin="0">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="260"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Left Sidebar -->
        <Border Grid.Column="0" Background="#14151F" BorderBrush="#242635" BorderThickness="0,0,1,0">
            <Grid Margin="16">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- Sidebar Header -->
                <StackPanel Grid.Row="0" Margin="0,0,0,24">
                    <Border Background="#FFFFFF" CornerRadius="6" Padding="10,8" Margin="0,0,0,12">
                        <Image Source="/LicenseTool;component/Assets/logo.png" Height="40" Stretch="Uniform" HorizontalAlignment="Center"/>
                    </Border>
                    <TextBlock Text="License Manager" FontSize="16" FontWeight="Bold" Foreground="#FFFFFF" HorizontalAlignment="Center" Margin="0,2,0,0"/>
                </StackPanel>

                <!-- Sidebar Content (Form & Actions) -->
                <StackPanel Grid.Row="1">
                    <!-- Server URL Box -->
                    <TextBlock Text="MÁY CHỦ LICENSE SERVER:" FontSize="10" FontWeight="Bold" Foreground="#8A8F9E" Margin="0,0,0,4"/>
                    <TextBox x:Name="ServerUrlBox" Text="http://localhost:5000" Style="{StaticResource ModernTextBox}" Margin="0,0,0,16"/>

                    <Border Height="1" Background="#242635" Margin="0,4,0,16"/>


                    <TextBlock Text="BẢN QUYỀN OFFLINE" FontSize="12" FontWeight="Bold" Foreground="#00D2FF" Margin="0,0,0,12"/>
                    <Button Style="{StaticResource ModernButton}" Content="Tạo Bản Quyền Offline" Click="OfflineGenerator_Click" Height="38" Background="#2F313E"/>
                </StackPanel>

                <!-- Sidebar Footer -->
                <StackPanel Grid.Row="2">
                    <Button Style="{StaticResource ModernButton}" Content="Tải lại dữ liệu (Refresh)" Click="Refresh_Click" Margin="0,0,0,12" Height="36"/>
                    <TextBlock x:Name="StatusBlock" Text="Sẵn sàng." FontSize="11" Foreground="#8A8F9E" TextWrapping="Wrap"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Right Content Area -->
        <Grid Grid.Column="1" Margin="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="3*"/> <!-- Licenses Grid (60%) -->
                <RowDefinition Height="Auto"/> <!-- Splitter / Gap -->
                <RowDefinition Height="2*"/> <!-- Devices Grid (40%) -->
            </Grid.RowDefinitions>

            <!-- Row 0: Licenses Grid -->
            <Border Grid.Row="0" Background="#181922" CornerRadius="8" Padding="16" BorderBrush="#242635" BorderThickness="1">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    
                    <TextBlock Text="DANH SÁCH LICENSE KEY" FontSize="14" FontWeight="Bold" Foreground="#00D2FF" Margin="0,0,0,12"/>
                    
                    <DataGrid x:Name="LicensesGrid" Grid.Row="1" AutoGenerateColumns="False" IsReadOnly="True" 
                               Background="Transparent" BorderThickness="0" RowBackground="#1C1D26" AlternatingRowBackground="#20222D"
                               Foreground="#FFFFFF" RowHeight="36" HeadersVisibility="Column" SelectionChanged="LicensesGrid_SelectionChanged"
                               GridLinesVisibility="None" SelectionMode="Single">
                        <DataGrid.Resources>
                            <Style TargetType="DataGridColumnHeader">
                                <Setter Property="Background" Value="#252736"/>
                                <Setter Property="Foreground" Value="#00D2FF"/>
                                <Setter Property="FontWeight" Value="SemiBold"/>
                                <Setter Property="Padding" Value="10,6"/>
                                <Setter Property="BorderThickness" Value="0"/>
                            </Style>
                            <Style TargetType="DataGridCell">
                                <Setter Property="Padding" Value="10,6"/>
                                <Setter Property="BorderThickness" Value="0"/>
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="DataGridCell">
                                            <Border Padding="{TemplateBinding Padding}" Background="{TemplateBinding Background}">
                                                <ContentPresenter VerticalAlignment="Center"/>
                                            </Border>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </DataGrid.Resources>
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="License Key" Binding="{Binding LicenseKey}" Width="200"/>
                            <DataGridTextColumn Header="Khách hàng" Binding="{Binding CustomerName}" Width="130"/>
                            <DataGridTextColumn Header="Sản phẩm" Binding="{Binding Product}" Width="80"/>
                            <DataGridTextColumn Header="Tính năng" Binding="{Binding Features}" Width="140"/>
                            <DataGridTextColumn Header="Loại" Binding="{Binding LicenseType}" Width="90"/>
                            <DataGridTextColumn Header="Trạng thái" Binding="{Binding Status}" Width="90"/>
                            <DataGridTextColumn Header="Giới hạn máy" Binding="{Binding MaxMachines}" Width="90"/>
                            <DataGridTextColumn Header="Ngày hết hạn" Binding="{Binding ExpireAtFormatted}" Width="150"/>
                            <DataGridTemplateColumn Header="Sao chép Key" Width="110">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <Button Content="Copy Key" Click="CopyKeyFromRow_Click" Padding="10,4" FontSize="11" Background="#2F3342" Foreground="#FFFFFF" BorderThickness="0" Cursor="Hand">
                                            <Button.Resources>
                                                <Style TargetType="Border">
                                                    <Setter Property="CornerRadius" Value="4"/>
                                                </Style>
                                            </Button.Resources>
                                        </Button>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </Border>

            <!-- Spacer Row 1 -->
            <Grid Grid.Row="1" Height="16"/>

            <!-- Row 2: Selected License Details & Devices -->
            <Border Grid.Row="2" Background="#181922" CornerRadius="8" Padding="16" BorderBrush="#242635" BorderThickness="1">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <!-- Row 2 Header -->
                    <Grid Grid.Row="0" Margin="0,0,0,12">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="THIẾT BỊ ĐÃ KÍCH HOẠT" FontSize="14" FontWeight="Bold" Foreground="#00D2FF" VerticalAlignment="Center"/>
                            <TextBlock x:Name="SelectedKeyLabel" Text="" FontSize="13" Foreground="#8A8F9E" Margin="10,0,0,0" FontWeight="Medium" VerticalAlignment="Center"/>
                        </StackPanel>
                        
                        <Button x:Name="RevokeLicenseBtn" Grid.Column="1" Style="{StaticResource DangerButton}" Content="Thu hồi toàn bộ Key" Click="RevokeLicense_Click" IsEnabled="False" Padding="12,6" FontSize="12"/>
                    </Grid>

                    <!-- Devices Table -->
                    <DataGrid x:Name="MachinesGrid" Grid.Row="1" AutoGenerateColumns="False" IsReadOnly="True" 
                               Background="Transparent" BorderThickness="0" RowBackground="#1C1D26" AlternatingRowBackground="#20222D"
                               Foreground="#FFFFFF" RowHeight="36" HeadersVisibility="Column"
                               GridLinesVisibility="None" SelectionMode="Single">
                        <DataGrid.Resources>
                            <Style TargetType="DataGridColumnHeader">
                                <Setter Property="Background" Value="#252736"/>
                                <Setter Property="Foreground" Value="#00D2FF"/>
                                <Setter Property="FontWeight" Value="SemiBold"/>
                                <Setter Property="Padding" Value="10,6"/>
                                <Setter Property="BorderThickness" Value="0"/>
                            </Style>
                            <Style TargetType="DataGridCell">
                                <Setter Property="Padding" Value="10,6"/>
                                <Setter Property="BorderThickness" Value="0"/>
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="DataGridCell">
                                            <Border Padding="{TemplateBinding Padding}" Background="{TemplateBinding Background}">
                                                <ContentPresenter VerticalAlignment="Center"/>
                                            </Border>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </DataGrid.Resources>
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Thiết bị" Binding="{Binding FingerprintShort}" Width="90"/>
                            <DataGridTextColumn Header="Trạng thái" Binding="{Binding Status}" Width="90"/>
                            <DataGridTextColumn Header="Mã Machine ID / Fingerprint" Binding="{Binding MachineFingerprint}" Width="200"/>
                            <DataGridTextColumn Header="Motherboard" Binding="{Binding MotherboardSerial}" Width="120"/>
                            <DataGridTextColumn Header="BIOS" Binding="{Binding BiosSerial}" Width="120"/>
                            <DataGridTextColumn Header="Ngày kích hoạt" Binding="{Binding CreatedAtFormatted}" Width="140"/>
                            <DataGridTextColumn Header="Heartbeat cuối" Binding="{Binding LastSeenAtFormatted}" Width="150"/>
                            <DataGridTemplateColumn Header="Thao tác" Width="140">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                            <Button Content="Thu hồi" Click="RevokeMachineFromRow_Click" Margin="0,0,6,0" Padding="6,3" FontSize="11" Background="#FF4D4D" Foreground="#FFFFFF" BorderThickness="0" Cursor="Hand">
                                                <Button.Resources>
                                                    <Style TargetType="Border">
                                                        <Setter Property="CornerRadius" Value="4"/>
                                                    </Style>
                                                </Button.Resources>
                                            </Button>
                                            <Button Content="Reset" Click="ResetMachineFromRow_Click" Padding="6,3" FontSize="11" Background="#2F3342" Foreground="#FFFFFF" BorderThickness="0" Cursor="Hand">
                                                <Button.Resources>
                                                    <Style TargetType="Border">
                                                        <Setter Property="CornerRadius" Value="4"/>
                                                    </Style>
                                                </Button.Resources>
                                            </Button>
                                        </StackPanel>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</Window>
